Adds ``firstname`` and ``lastname`` fields to ``event.registration``.

The ``name`` field is kept in sync using the same order rules as
``partner_firstname`` (``partner_names_order`` system parameter).
